# Data

The application accepts candidate resumes at runtime and does not require private candidate documents to be committed to the repository.

## Supported formats

- PDF
- DOCX
- TXT

For experiments, use synthetic or publicly licensed resume/job-description datasets. Do not commit personally identifiable candidate information to this directory.

The current MVP performs feature extraction directly from uploaded documents and does not require a pre-trained model artifact.